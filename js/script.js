// Webcam Preview button (demo action)
const enableBtn = document.getElementById("enableCameraBtn");

enableBtn.addEventListener("click", () => {
  alert("Webcam preview will be enabled on the Attendance page!");
});
